language: dart
dart:
  # Install the latest stable release
  - stable
dart_task:
  - test: --exclude-tags no-xvfb
    xvfb: false
script:
  pub run test test/grove_temperature_test.dart
  pub run test test/grove_light_test.dart
  pub run test test/grove_pir_test.dart
  pub run test test/grove_sound_test.dart

branches:
    only:
    - master

